# Public data

This folder contains redacted data and frozen outputs used for manuscript reproduction.

Full abstract text and sentence text are not redistributed. The sentence-level public files preserve PMIDs, sentence identifiers, labels, confidence values, and derived metrics where available.

The `supplementary_outputs/frozen_source_zips/` folder preserves the original analysis-output zip files used to build the public release.
